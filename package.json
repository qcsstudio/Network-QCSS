{
  "name": "network-command-growth-os",
  "version": "0.1.0",
  "private": true,
  "description": "Consent-aware marketing, lead generation, assessment, and admin MVP for network services and training.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "verify": "node verify.js",
    "check": "node --check server.js && node --check script.js && node --check admin.js"
  },
  "engines": {
    "node": ">=18"
  }
}
